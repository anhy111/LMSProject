<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace : xml파일은 여러개일 수 있음. 
이를 구별하기 위한 식별 용도로 사용 -->
<mapper namespace="kr.or.ddit.mapper.RecordMapper">


<!-- 신청 -->
<insert id="RecordApply" parameterType="Record" >
INSERT INTO RECORD (
		  REC_CD
		, STU_NO
		, RGB_CD
		, REC_YR
		, REC_SEM
		, REC_DT
		, REC_PER
		, REC_RSN
		, REC_YN
		, REC_REJ
		)
VALUES(
		RECORD_SEQ.NEXTVAL
		, #{stuNo}
		, #{rgbCd}
		, TO_NUMBER(TO_CHAR(SYSDATE,'YYYY'))
		, #{recSem}
		, SYSDATE
		<if test="recPer != null and recPer != '' ">, #{recPer}</if>
		<if test="recRsn != null and recRsn != '' ">, #{recRsn}</if>
		<if test="recYn != null and recYn != '' ">, #{recYn}</if>
		<if test="recRej != null and recRej != '' ">, #{recRej}</if>
		)
</insert>
<!-- 학적 변동 현황 조회(신청 목록 조회) -->
<select id="RecordList" parameterType="int" resultType="Record">
SELECT 
  REC_CD
, R.STU_NO
, R.RGB_CD
, R.REC_YR
, R.REC_SEM
, R.REC_PER
, R.REC_RSN
, R.REC_DT
, R.REC_YN
FROM RECORD R, STUDENT S
WHERE R.STU_NO = #{stuNo}
AND R.STU_NO = S.STU_NO
</select>
</mapper> 

