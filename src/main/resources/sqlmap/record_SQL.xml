<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace : xml파일은 여러개일 수 있음. 
이를 구별하기 위한 식별 용도로 사용 -->
<mapper namespace="kr.or.ddit.mapper.RecordMapper">


<!-- 신청 -->
<insert id="RecordApply" parameterType="Record" >
INSERT INTO RECORD (
		  REC_CD
		, STU_NO
		, RGB_CD
		, REC_YR
		, REC_SEM
		, REC_DT
		, REC_YN
		, REC_PER
		, REC_RSN
		)
VALUES(
		RECORD_SEQ.NEXTVAL
		, #{stuNo}
		, #{rgbCd}
		<if test="recYr == null or recYr =='' ">, TO_NUMBER(TO_CHAR(SYSDATE,'YYYY'))</if>
		, #{recSem}
		<if test="recDt == null or recDt =='' ">, SYSDATE</if>
		, #{recYn}
		<if test="recPer != null or recPer != '' ">, #{recPer}</if>
		<if test="recRsn != null or recRsn != '' ">, #{recRsn}</if>
		)
</insert>
<!-- 학적 변동 현황 조회(신청 목록 조회) -->
<select id="RecordList" parameterType="int" resultType="Record">
SELECT 
  R.REC_CD
, R.STU_NO
, R.RGB_CD
, R.REC_YR
, R.REC_SEM
, R.REC_PER
, R.REC_RSN
, R.REC_DT
, CD.COMD_NM AS REC_YN
, R.REC_REJ
FROM RECORD R, STUDENT S, COMMON_DETAIL CD, COMMON C
WHERE R.STU_NO = S.STU_NO
AND CD.COM_CD = C.COM_CD
AND CD.COMD_CD = R.REC_YN
AND R.STU_NO = #{stuNo}
</select>

<!--[상담테이블] (신청 목록 조회) 단, 상담에 추가된 학적신청 번호는 제외 -->
<select id="counselFilteredRecordList" parameterType="int" resultType="Record">
SELECT 
  R.REC_CD
, R.STU_NO
, R.RGB_CD
, R.REC_YR
, R.REC_SEM
, R.REC_PER
, R.REC_RSN
, R.REC_DT
, CD.COMD_NM AS REC_YN
, R.REC_REJ
FROM RECORD R LEFT OUTER JOIN COUNSEL CS ON R.REC_CD = CS.REC_CD, STUDENT S, COMMON_DETAIL CD, COMMON C
WHERE R.STU_NO = S.STU_NO
AND CD.COM_CD = C.COM_CD
AND CD.COMD_CD = R.REC_YN
AND R.STU_NO = #{stuNo}
AND CS.REC_CD IS NULL
</select>
</mapper> 

