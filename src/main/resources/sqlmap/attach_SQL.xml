<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace : xml파일은 여러개일 수 있음. 
이를 구별하기 위한 식별 용도로 사용 -->
<mapper namespace="kr.or.ddit.mapper.AttachMapper">


<insert id="insertAttch" parameterType="attach">
insert into attach(ATCH_FILE_ID, FILE_SN, FILE_STRE_COURS, STRE_FILE_NM, ORIGNL_FILE_NM, FILE_EXTSN, FILE_SIZE, CREATE_DT)
    values((select decode(nvl(max(atch_file_id)+1,0),0,1)
from attach),#{fileSn},#{fileStreCours},#{streFileNm},#{orignlFileNm},#{fileExtsn},#{fileSize},SYSDATE)
</insert >


</mapper>

