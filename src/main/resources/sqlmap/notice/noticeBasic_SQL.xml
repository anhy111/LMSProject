<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace : xml파일은 여러개일 수 있음.
이를 구별하기 위한 식별 용도로 사용 -->
<mapper namespace="kr.or.ddit.mapper.NoticeBasicMapper">


<!--    &lt;!&ndash; 공지사항 목록 조회 &ndash;&gt;-->
<!--    <select id="noticeBasicList" resultType="NoticeBasic">-->

<!--        SELECT NOTICE_CD,-->
<!--               NOTICE_TTL,-->
<!--               NOTICE_CON,-->
<!--               NOTICE_REG,-->
<!--               NOTICE_UPD-->
<!--        FROM NOTICE_BASIC-->

<!--    </select>-->

<!--    &lt;!&ndash; 공지사항 상세페이지 조회 &ndash;&gt;-->
<!--    <select id="noticeBasicFindNotice" resultType="NoticeBasic">-->
<!--        SELECT *-->
<!--        FROM NOTICE_BASIC-->
<!--        WHERE NOTICE_CD = #{noticeCd}-->
<!--    </select>-->

    <!-- 공지사항 등록 -->
    <insert id="noticeBasicSave" parameterType="NoticeBasic">

        INSERT INTO NOTICE_BASIC
        VALUES
            (
            NOTICEBASIC_SEQ.NEXTVAL,
            #{noticeTtl},
            SYSDATE,
            #{noticeCon}
            )
    </insert>

    <!-- 공지사항 수정 -->
    <update id="noticeBasicUpdate" parameterType="NoticeBasic">
        UPDATE NOTICE_BASIC
        SET
            NOTICE_TTL = #{noticeTtl},
            NOTICE_CON = #{noticeCon},
            NOTICE_REG = SYSDATE
        WHERE
            NOTICE_CD = #{noticeCd}
    </update>

    <!-- 공지사항 삭제 -->
    <delete id="noticeBasicDelete">
        DELETE
        FROM NOTICE_BASIC
        WHERE NOTICE_CD = #{noticetCd}
    </delete>

</mapper>










